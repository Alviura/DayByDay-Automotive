<style>
    .prf-page .mi-page-icon {
        background: linear-gradient(135deg, #06b6d4, #0891b2);
    }

    .prf-hero {
        display: flex; align-items: flex-start; gap: 1rem; min-width: 0;
    }
    .prf-avatar {
        width: 4rem; height: 4rem; border-radius: 16px; flex-shrink: 0;
        display: flex; align-items: center; justify-content: center;
        font-size: 1.25rem; font-weight: 800; color: #fff;
        background: linear-gradient(135deg, #f97316, #dc2626);
        border: 3px solid #fff; box-shadow: 0 4px 14px rgba(249,115,22,.25);
    }

    .prf-account-card {
        border: 1px dashed #bae6fd;
        background: linear-gradient(135deg, #f0f9ff, #ecfeff);
        border-radius: 12px; padding: 1rem; margin-bottom: 1rem;
    }
    .prf-account-label {
        font-size: .62rem; font-weight: 700; letter-spacing: .06em;
        text-transform: uppercase; color: #0891b2; margin-bottom: .35rem;
    }

    .prf-danger-card {
        border-color: #fecaca !important;
        background: linear-gradient(135deg, #fffbfb, #fff1f2);
    }
    .prf-danger-head {
        border-bottom-color: #fecaca !important;
        background: rgba(255,255,255,.6);
    }
</style>
